export enum RoleEnum {
  CLIENT = 'client',
  OWNER = 'owner',
  ADMIN = 'admin',
}

export enum RedisKeys {}

export enum StatusEnum {
  DEBIT = 'debit',
  CREDIT = 'credit',
}

export enum StatusTrackeEnum {
  CREATED = 'created',
  PROGRESS = 'progress',
  DONE = 'done',
}

export enum RedisKeys {
  ALL_USERS = 'ALL_USERS',
  All_FILES = "ALL_FILES"
}
