export enum RoleEnum {
  CLIENT = 'client',
  OWNER = 'owner',
  ADMIN = 'admin',
}

export enum RedisKeys {}

export enum StatusEnum {
  DEBIT = 'debit',
  CREDIT = 'credit',
}

export enum StatusTrackeEnum {
  CREATED = 'created',
  PROGRESS = 'progress',
  DONE = 'done',
}
